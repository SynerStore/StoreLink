export enum StoreTypes {
  S3 = 's3',
  OSS = 'oss',
  FTP = 'ftp',
  SFTP = 'sftp',
  LOCAL = 'local',
}

export enum StoreConnectStatus {
  unexec = 'unexec',
  success = 'success',
  fail = 'fail',
}
